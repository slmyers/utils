export interface Data {
    queryMappings: Object,
    queries: Object[],
    screener: Object,
    programs: Object[]
}

export interface Mapping {
    programMappings: Object,
    queryMappings: Object,
    screenerMappings: Object
}